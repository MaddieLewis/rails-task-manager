<h1>Index</h1>

<h2>Should show all tasks</h2>

<% @tasks.each do |task| %>
  <h1><%= link_to task.title, task_path(task) %></h1>
  <p><%= task.details %></p>
  <p><%= task.completed %></p>
<% end %>


<%# link_to "Delete", tasks_path(task), method: :delete, data: { confirm: 'Are you sure?' } %>
